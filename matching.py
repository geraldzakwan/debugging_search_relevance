# Will return product IDs using AND logic to
# aggregate results from various fields
# Now feature is limited to string only
def match(indexes, fields, search_terms):
    for field in fields:
        pass

if __name__ == '__main__':
    pass
